# Training locale Abra — Gemma 4 + Unsloth (RTX 5070)
# Installato da setup-abra-ai-local.ps1 nel venv .venv-ai

torch>=2.4.0
transformers>=4.45.0
datasets>=3.0.0
trl>=0.12.0
peft>=0.13.0
accelerate>=1.0.0
bitsandbytes>=0.44.0; platform_system != "Windows"
# Unsloth — install separato con index URL (vedi train-abra-gemma.ps1)
